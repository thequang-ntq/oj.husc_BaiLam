#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int32_t main() {
 ll a, b, c, d, s;cin >> a >> b >> c >> d;
    s = (a % 100 * b % 100 * c % 100 * d % 100) % 100;
    cout << setw(2) << setfill('0') << s << endl;
    
    return 0;
}
